---
- name: Update Customer AWS Security Group Rules
  hosts: localhost
  tasks:
    - name: Call role
      include_role:
        name: aws-sg-upgrade
      vars:
        vpc_region: us-east-1
        vpc_id: vpc-0676cad4b800ca1fa
