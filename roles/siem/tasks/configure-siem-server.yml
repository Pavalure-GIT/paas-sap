- name: install PIP
  easy_install:
    name: "pip"
    state: latest

- name: install wget
  yum:
    name: ['wget', 'dos2unix']
    state: present

- name: install boto3 and botocore    
  pip:
    name: ['botocore', 'boto3']
    
- name: download crowdstrike package
  aws_s3:
    bucket: siem-config-system-team
    object: /crowdstrike-siem.rpm
    dest: /tmp/crowdstrike-siem.rpm
    mode: get
    aws_access_key: "{{ lookup('env','MEDIA_ACCESS_KEY') }}"
    aws_secret_key: "{{ lookup('env','MEDIA_SECRET_KEY') }}"

- name: installing siem connector
  yum:
    name: /tmp/crowdstrike-siem.rpm
    state: present

- name: download crowdstrike config
  aws_s3:
    bucket: siem-config-system-team
    object: /cs.falconhoseclient.cfg
    dest: /opt/crowdstrike/etc/cs.falconhoseclient.cfg
    mode: get
    aws_access_key: "{{ lookup('env','MEDIA_ACCESS_KEY') }}"
    aws_secret_key: "{{ lookup('env','MEDIA_SECRET_KEY') }}"

- name: run dos2unix on config file
  command: dos2unix /opt/crowdstrike/etc/cs.falconhoseclient.cfg



# - name: edit cs.falconhoseclient.cfg api key
#   lineinfile:
#     path: /opt/crowdstrike/etc/cs.falconhoseclient.cfg
#     regexp: '^api_key ='
#     line: api_key = {{ crowdstrike_api_key }}

# - name: edit cs.falconhoseclient.cfg api uuid
#   lineinfile:
#     path: /opt/crowdstrike/etc/cs.falconhoseclient.cfg
#     regexp: '^api_uuid ='
#     line: api_uuid = {{ crowdstrike_api_uuid }}

- name: start the crowdstrike service
  service:
    name: cs.falconhoseclientd
    state: started 

- name: install policy core utils python
  yum:
    name: policycoreutils-python
    state: present

- name: download cloud watch Agent
  get_url:
    url: https://s3.amazonaws.com/amazoncloudwatch-agent/redhat/amd64/latest/amazon-cloudwatch-agent.rpm
    dest: /tmp/amazon-cloudwatch-agent.rpm

- name: install amazon-cloudwatch-agent.rpm
  yum:
    name: /tmp/amazon-cloudwatch-agent.rpm
    state: present

#check that the /var/log/crowdstrike/falconhoseclient/cs.falconhoseclient.log exists
- name: download cloudwatch config
  aws_s3:
    bucket: siem-config-system-team
    object: /e4s-siem-cloudwatch-config.json
    dest: /tmp/e4s-siem-cloudwatch-config.json
    mode: get
    aws_access_key: "{{ lookup('env','MEDIA_ACCESS_KEY') }}"
    aws_secret_key: "{{ lookup('env','MEDIA_SECRET_KEY') }}"

- name: restart cloudwatch with json file
  shell: "/opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl -a fetch-config -m ec2 -c file:/tmp/e4s-siem-cloudwatch-config.json -s "



